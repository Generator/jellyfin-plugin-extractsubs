name: '🧪 Test Plugin'

on:
  push:
    branches:
      - dev
    paths-ignore:
      - 'manifest.json'
  pull_request:
    branches:
      - dev
    paths-ignore:
      - 'manifest.json'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: ['10.0.x']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Build Plugin
        run: dotnet build -c Release Jellyfin.Plugin.SubtitleExtract/Jellyfin.Plugin.SubtitleExtract.csproj

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-build-net${{ matrix.dotnet-version }}
          path: |
            Jellyfin.Plugin.SubtitleExtract/bin/Release/net*/Jellyfin.Plugin.SubtitleExtract.dll
            Jellyfin.Plugin.SubtitleExtract/bin/Release/net*/Jellyfin.Plugin.SubtitleExtract.pdb
          retention-days: 14
